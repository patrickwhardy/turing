<h1>All Tools</h1>

<div class="notice">
  <%= flash[:notice] %>
</div>

<%= link_to "Create New Tool", new_tool_path %>

<% @tools.each do |tool| %>
  <p><%= link_to "#{tool.name}", tool_path(tool.id) %>
  <%= tool.quantity %></p>
<% end %>
